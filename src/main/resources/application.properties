server.port=${PORT:8000}
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost/passmanager-db}
spring.datasource.username=${DATABASE_USERNAME:passmanager}
spring.datasource.password=${DATABASE_PASSWORD:afzGiNNoUrtJQ3IEf9TF}
# Spring Data JPA
spring.jpa.hibernate.ddl-auto=${DATABASE_DDL:create}
spring.jpa.show-sql=${DATABASE_PROD:true}
spring.jpa.properties.hibernate.format_sql=true
#jwt.secret=passmanager-secret-key
heroku.enforceHttps=true